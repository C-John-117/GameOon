<UserControl x:Class="Game_On.Views.PageConnexion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Game_On.Views"
             xmlns:viewmodel="clr-namespace:Game_On.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodel:ConnexionVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Margin="10" Content="nom: " FontSize="15" />
            <TextBox x:Name="pseudo" Grid.Column="1" Width="175" Margin="10" HorizontalContentAlignment="Left" Text="{Binding Pseudo}"/>

        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Label Margin="10" Grid.Column="0" Content="Mot de passe: " FontSize="15"/>
            <PasswordBox x:Name="motdepasse" Grid.Column="1" Width="175" Margin="10" PasswordChanged="PasswordBox_PasswordChanged"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Click="btn_connexion_Click"
                    Content="Connexion" x:Name="btn_connexion" 
                    Grid.Column="0" Height="30" Margin="10,0"  IsDefault="True"/>
            <Button Click="btn_inscription_Click" Content="S'inscrire" x:Name="btn_inscription" Grid.Column="1" Height="30" Margin="10,0"/>
        </Grid>
    </Grid>
</UserControl>
